export function setMeta (state, { meta }) {
  state.meta = meta
}

export function setSlides (state, { slides }) {
  state.slides = slides
}

export function setPage (state, { page }) {
  state.page = page
}
